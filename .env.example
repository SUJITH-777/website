# Optional: canonical site URL with no trailing slash (for Open Graph / Twitter cards).
# On Vercel builds, VERCEL_URL is used automatically if this is unset.
# VITE_SITE_BASE=https://www.example.com
